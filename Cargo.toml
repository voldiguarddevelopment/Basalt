[workspace]
resolver = "2"
members = ["crates/*"]

[workspace.package]
version = "0.0.0"
edition = "2021"

[profile.release]
# Determinism: no incremental, reproducible artifacts (invariant 5).
incremental = false
